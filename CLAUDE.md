# CLAUDE.md

このファイルはClaude Codeがこのリポジトリで作業する際のガイドラインを定義します。

## プロジェクト概要

タスクスケジュール管理アプリケーション。

## Git 運用ルール

### 基本方針

- **コードを変更するたびに、必ずGitHubにプッシュすること。**
- コミットは意味のある単位でまとめ、作業途中の中途半端な状態でコミットしない。
- `main` ブランチへの直接プッシュは原則禁止。機能追加・バグ修正は必ずブランチを切ること。

### コミットメッセージ規則

プレフィックスを使ってコミットの種類を明示する：

| プレフィックス | 用途 |
|---|---|
| `feat:` | 新機能の追加 |
| `fix:` | バグ修正 |
| `refactor:` | リファクタリング（機能変更なし） |
| `docs:` | ドキュメント変更 |
| `test:` | テストの追加・修正 |
| `chore:` | ビルド設定・依存関係の更新など |

例：
```
feat: タスクの繰り返しスケジュール機能を追加
fix: 期限切れタスクが正しく表示されない問題を修正
```

### プッシュのタイミング

以下のタイミングで必ずプッシュすること：

1. 機能の実装が完了したとき
2. バグ修正が完了したとき
3. セッション終了前
4. レビュー依頼前

### ブランチ命名規則

```
feature/<機能名>     # 新機能
fix/<バグ内容>       # バグ修正
refactor/<対象>      # リファクタリング
```

### プッシュ手順

```bash
git add <変更ファイル>
git commit -m "<プレフィックス>: <変更内容>"
git push origin <ブランチ名>
```

## コーディング規則

- コメントは「なぜそうするか（WHY）」が自明でない場合のみ記述する。
- 不要なコード・デッドコードは削除する。
- セキュリティ脆弱性（SQLインジェクション、XSS等）を導入しない。

## 作業フロー

1. タスクを確認・整理する
2. ブランチを作成する
3. 実装・テストを行う
4. コミット＆GitHubにプッシュする
5. 必要に応じてPull Requestを作成する
